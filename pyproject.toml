[project]
name = "faststream-bridge"
version = "0.1.0"
description = "Add your description here"
requires-python = ">=3.13"
dependencies = [
    "fastapi>=0.137.0",
    "faststream[rabbit]>=0.7.1",
    "uvicorn>=0.49.0",
]
